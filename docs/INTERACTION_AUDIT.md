# 全应用交互审计

## 2026-07-22 数据口径与资产目标复查

| 页面/流程 | 健康度 | 验证与处理 |
|---|---|---|
| 交易快速筛选 | 通过 | 账户、类型和类别控件均打开真实选择器；所选条件同时驱动列表和三张口径卡，不再只改变行列表。 |
| 账户资产目标 | 通过 | 头部旗标和摘要卡均打开 `AssetGoalsPage`；新增、编辑、删除和返回都调用真实 provider/mutation，进度按不扣信用卡及贷款的总资产计算。 |
| 信用账户记录还款 | 通过 | 按钮打开真实同币种现金账户列表；选中后确认或调整金额，保存为现金账户转入信用账户的实际转账。 |
| 预算生效月份 | 通过 | 月份选择器保存真实 `monthKey`；改到新月份时创建新规则并保留旧月份金额，说明文字明确延续范围。 |
| 报表区间与指标 | 通过 | 区间控件仍真实刷新；所有收支指标改用实际现金派生查询，点击健康信号继续进入详细报表。 |


## 2026-07-21 账户统计截止专项复查

上一轮源码门禁只拒绝 `_noop` 和空回调，无法识别“显示方向箭头但根本没有回调”的视觉假入口，因此曾把账户页“统计截止”误判为通过。本轮先在 Windows Debug 应用复现点击无响应，再补齐行为，并把方向性图标附近必须存在真实交互处理器加入防回归测试。

| 步骤 | 页面/流程 | 健康度 | 本轮验证与处理 |
|---|---|---|---|
| 1 | 账户页现状复现 | 修复前失败 | 点击“统计截止”后界面无菜单、无状态变化；证据为 [点击前](../artifacts/interaction-audit/2026-07-21-cutoff/01-before-account-cutoff.png) 和 [点击后无响应](../artifacts/interaction-audit/2026-07-21-cutoff/02-before-cutoff-no-response.png)。 |
| 2 | 截止月份选择 | 通过 | 整行成为语义按钮；打开从最早交易、快照或贷款追踪月份连续到当前月的底部列表，不允许未来月份。整改后证据见 [月份选择器](../artifacts/interaction-audit/2026-07-21-cutoff/03-after-cutoff-picker.png)。 |
| 3 | 历史账户汇总 | 通过 | 选择历史月份后，现金、投资、退休、信用卡和贷款汇总统一按当月月末计算，并显示历史截止提示。证据见 [历史截止状态](../artifacts/interaction-audit/2026-07-21-cutoff/04-after-historical-cutoff.png)。 |
| 4 | 历史详情继承 | 通过 | 普通、投资、信用卡和贷款详情继承同一 `cutoffDate`，只展示截止日前的实际资料；编辑、删除、还款、补生成和录入市值入口隐藏，返回本月后恢复。 |
| 5 | 方向性控件扫描 | 通过 | 移除账户/类别输入框、禁用 Google 登录和只读主题预览的误导箭头；类别收入/支出标题成为真实的展开/折叠控件。 |
| 6 | 静态与 Widget 门禁 | 通过 | `interaction_wiring_test.dart` 同时拒绝空回调和没有附近处理器的方向性图标，并验证类别折叠；账户截止 Widget 用例验证月份选择、金额变化、只读横幅和返回本月。 |

## 全应用交互契约

- 箭头、下拉符号、可点击卡片和启用状态的按钮必须具有真实处理逻辑。
- 纯信息行不得显示导航箭头，也不得用空回调伪装可点击状态。
- 尚未实现的能力必须显示“计划中”或限制说明，并使用禁用状态。
- 发生持久化写入后必须刷新 `financeRepositoryProvider`，让六个主页面读取同一份最新快照。
- 周期规则未来展开只写入 `planned`，不提前改变账户实际余额。
- 历史统计截止只读页面不得暴露任何会改写当前账本的操作。

## 证据边界

本轮证据包括 Windows 实机页面截图、代码逐项对照、390×844 Widget 交互回归、全量测试和静态分析。截图与 Widget 测试不能完整证明屏幕阅读器、键盘遍历、超大字体和所有桌面尺寸的 WCAG 合规；系统级通知调度、Google 登录、系统主题跟随和交易附件仍为明确标注的计划能力。
